jobs:
- job: ExampleJob
  displayName: 'Publish to Azure Service Bus'
  pool:
    vmImage: 'ubuntu-latest'  # Use an appropriate Ubuntu version
  steps:
  - task: AzureCLI@2
    inputs:
      azureSubscription: 'SF3358-Bold BI-DevOps-Ranjithkumar(d85cca03-706a-4ff7-81bf-d0a90d9bc685)'
      scriptType: 'bash'
      scriptLocation: 'inlineScript'
      inlineScript: |
        # Azure CLI commands to publish to Azure Service Bus
        az servicebus queue send --namespace-name ranjithtesting --queue-name ranjith --message-body "Message vandhruchae" --sign-payload false --data-contract-serializer false


  

  