jobs:
- job: ExampleJob1
  displayName: 'Publish to Azure Service Bus'
  pool: server
  steps:
    - task: PublishToAzureServiceBus@2
      inputs:
        azureSubscription: 'Ranjithazurebusre'
        serviceBusQueueName: 'testingpipeline'
        serviceBusNamespace: 'azurebusrk'
        messageBody: 'noenwnnen'
        signPayload: false
        waitForCompletion: false
        useDataContractSerializer: true
        signatureKey: '$(buskey)'
    
